---

provisioning:
  provider: vsphere
  controller:
    prefix: controller-
    count: 3
  worker:
    prefix: worker-
    count: 2
  ssh_user: root
  ssh_private_key: "~/id_rsa_user"
  use_volumes: true

---

hcloud:
  cli_version: 1.13.0
  cli_path: ~/.local/bin/

provisioning:
  provider: hcloud
  controller:
    prefix: vl-k8sbld-c-
    count: 1
  worker:
    prefix: vl-k8sbld-w-
    count: 1
  ssh_user: root
  ssh_private_key: "~/id_rsa_hetzner"
  use_volumes: true
